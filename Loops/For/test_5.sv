module test_5();

/*
Exercise 5 - Compare Two Memories

Module
memory_compare

Input

mem_a (32 elements, each 16 bits)
mem_b (32 elements, each 16 bits)

Output

equal (1 bit)

Requirements

Use a for loop to compare the two memories.

Set equal to 1 if all elements are identical.
Set equal to 0 if any element is different.

*/

endmodule