module test_18();

/*
Exercise 18 - Vector to Structure

Requirements:
- Create a packed struct named status_t.
- Fields:
    * error   (1 bit)
    * ready   (1 bit)
    * mode    (2 bits)
    * counter (12 bits)

- The total struct width must be 16 bits.
- Declare one status_t variable.
- Declare one 16-bit vector named status_word.
- Assign a value to status_word.
- Decode status_word into the structure.
- Display every decoded field.

*/

endmodule